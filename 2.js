function createCircle(r) {
  return Math.PI * Math.pow(r, 2);
}
let S1 = createCircle(3);
let S2 = createCircle(7);
let S3 = createCircle(1);
console.log(`1-doira yuzi  ${S1} 2-doira yuzi ${S2} 3-doira yuzi ${S3} `);
