function Max(x, y, z) {
  return Math.max(x, y, z);
}
function Min(x, y, z) {
  return Math.min(x, y, z);
}
let max = Max(3.96, 3.58, 2.83);
let min = Min(3.96, 3.58, 2.83);
console.log(max);
console.log(min);
